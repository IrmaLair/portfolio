services:
  - type: static
    name: portfolio
    repo: https://github.com/IrmaLair/portfolio
    branch: master
  buildCommand: "$ build"
    publishPath: "."    # publish root where index.html lives
  startCommand: "$ build-start"
    env: {}
